{
  "content": [
    {
      "type": "text",
      "text": "# AGENTS.md\n\nThis file provides context and instructions for AI coding agents working on the pandya.ai Game Platform.\n\n## Project Overview\npandya.ai is a highly extensible, real-time multiplayer board game platform. \n- **Backend**: Go (monolith), ConnectRPC, PostgreSQL.\n- **Frontend**: React.\n- **Game Engine**: Custom Go-based state machine that executes Lua scripts for game logic.\n- **AI Integration**: The platform features a multi-agent architecture (Logic Agent, UI Agent, Commentator Agent) orchestrated via the Google Agent Development Kit (ADK).\n\n## Core Agents\n\n### 1. Supervisor Agent\n- **Implementation**: `internal/agents/supervisor_agent.go`\n- **Role**: Coordinates the entire game authoring generation lifecycle.\n- **Key Features**:\n    - **Hierarchical Orchestration**: Orchestrates visual GDL layout, Lua rules logic, and React board UI compilations by calling worker agents via structured tool declarations.\n    - **Structured Game Planning (Phase 0)**: Before writing code, prompts the LLM to generate a comprehensive markdown game plan and implementation checklist based on rules, configuration, and player turn schemas.\n    - **Plan Alignment Validation (`validateAgainstPlan`)**: Compares outputs of compile steps against the original markdown plan to catch discrepancies (like missing win conditions or player count mismatch).\n    - **Self-Correction Retry Loop**: Evaluates sandbox engine stack traces, validation logs, and plan alignment errors, dynamically re-triggering worker agents with structured repair instructions.\n    - **Active Telemetry**: Generates OpenTelemetry spans tagged with OpenInference agent semantic conventions (`openinference.span.kind = \"AGENT\"`) capturing complete Blackboard state, model runs, and token cost metrics.\n\n### 2. Logic Agent\n- **Implementation**: `internal/agents/logic_agent.go`\n- **Role**: Translates natural language game descriptions into executable Lua scripts.\n- **Key Features**:\n    - **Lua Generation**: Generates complete game state machines including `setup`, `on_move`, `check_win`, and `get_actions` hooks.\n    - **Validation Loop & Contract Checks**: Uses `internal/gameauthor/validator.go` to enforce validation checks before code commits:\n        - **Logic Simulation**: Runs dynamic multi-turn simulations using `luaengine` to assert that the script completes execution, places/moves pieces successfully, and initializes card hands (if deck systems are used).\n        - **Action ID Alignment**: Checks that all player actions defined in `get_actions` map to the visual handlers.\n        - **Contract Consistency**: Verifies move payload parameter layouts (e.g. mapping `piece_id` or `card_id` appropriately) to prevent backend data mismatches.\n\n### 3. UI Agent\n- **Implementation**: `internal/agents/ui_agent.go`\n- **Role**: Generates dynamic React + TypeScript code for game boards.\n- **Key Features**:\n    - **Canvas Rendering**: Produces code utilizing `react-konva` for performant, declarative canvas interfaces.\n    - **Contract Sanitization & Validation**: Sanitizes code against the linter rules in `internal/gameauthor/validator.go`:\n        - **Flat Payload Structures**: Assures custom moves are passed as flat parameter dictionaries rather than nested under `payload:` objects.\n        - **snake_case Field Access**: Enforces snake_case field access to prevent PascalCase mismatches (e.g., alert on `.DefinitionID` vs `.definition_id`).\n        - **Safe Visual Accesses**: Requires safe optional chaining (e.g. `?.visual?.borderColor` instead of `.visual.borderColor`) to avoid client-side crashes on empty definitions.\n    - **Shared Component Library**: Accesses a global library of high-fidelity primitives (`<Deck />`, `<Dice />`, `<CardStack />`, `<Token />`, `<Pawn />`, `<Card />`, `<Tile />`, `<PlayingCard />`, `<StandardDeck />`, `<PlayingCardHand />`) for standardized mechanics.\n    - **Interactive Flow**: Implements complex interaction patterns (e.g., `selectPieceThenCell`) mapped to backend Action IDs.\n    - **Design System**: Enforces a \"rich\" aesthetic with glassmorphism, dark-mode optimization, and smooth micro-animations.\n\n### 4. Commentator Agent\n- **Implementation**: `internal/agents/commentator.go`\n- **Role**: Provides real-time, personality-driven game commentary.\n- **Key Features**:\n    - **Live Analysis**: Monitors the `GameManager` for state transitions and move events to generate relevant commentary.\n    - **Streaming Delivery**: Streams updates to connected clients via ConnectRPC, providing an \"alive\" feel to the match.\n    - **History Persistence**: Optionally stores commentary in PostgreSQL for review during game replays.\n\n### 5. Tournament Manager Agent\n- **Implementation**: `internal/agents/tournament_agent.go` and `internal/tournament/a2a.go`\n- **Role**: Orchestrates tournaments, manages matches, and coordinates players via chat.\n- **Key Features**:\n    - **A2A Exposure**: Integrates via the `/a2a/tournament/chat` endpoint to act as a conversational bot for tournament management.\n    - **Tool Calling**: Uses ADK tools to list, create, and delete tournaments and individual matches.\n    - **Link Generation**: Can generate shareable links and Markdown QR codes for participants to quickly join or spectate matches.\n    - **Contextual Management**: Communicates interactively to guide players throughout the lifecycle of an active tournament bracket.\n\n## External Agent Integrations (MCP & A2A)\n\nThe platform provides two primary ways for external general-purpose agents (like Antigravity or Claude) to interact with the Pandya engine.\n\n### 1. Model Context Protocol (MCP) Server\n- **Location**: \\`internal/mcp/server.go\\`\n- **Role**: Provides tools for local AI coding assistants to help developers manually write and test \\`logic.lua\\` and \\`component.tsx\\`.\n- **Tools Exposed**:\n  - \\`validate_game_code\\`: Triggers the Go backend's \\`GenerationValidator\\` to statically check for contract mismatches between Lua and React code.\n  - \\`simulate_game_logic\\`: Triggers the Go backend's \\`luaengine.SimulateGameplay\\` to run a headless, automated error-checking loop on local Lua code.\n  - \\`get_lua_libraries\\`: Reads \\`docs/INTERNAL_ENGINE.md\\` to provide context on available Lua hooks.\n  - \\`get_ui_components\\`: Dynamically reads \\`ui/src/game-components/index.ts\\` to provide the live catalog of React primitives.\n  - \\`get_authoring_guidelines\\`: Fetch standard guidelines.\n  - \\`scaffold_game\\`: Scaffold a new game directory.\n- **Usage**: The MCP server is integrated into the main backend server and exposed over SSE. \n  Configure your MCP client with the URL: \\`http://localhost:8080/mcp/sse\\`\n\n### 2. Agent-to-Agent (A2A) Protocol\n- **Location**: `internal/gameauthor/service.go`\n- **Role**: Allows external orchestrators (like Gemini Enterprise) to delegate autonomous game creation to the internal `SupervisorAgent` via standard OpenAI Chat Completions formats.\n- **Endpoints**:\n  - `GET /a2a/app/.well-known/agent-card.json`: Returns the Pandya Supervisor's identity card.\n  - `POST /a2a/chat`: Accepts a standard chat payload, parses the prompt, invokes `runGenerationPipeline`, captures the supervisor's streaming logs, and returns a detailed markdown summary block.\n- **Usage**: Bypasses the strict JWT ConnectRPC interceptors for local testing; responds directly with generated game status and logs.\n\n## Shared Memory & Communication Architectures\n\n### 1. In-Memory Blackboard\n- **Implementation**: `internal/agents/blackboard.go`\n- **Role**: Acts as a transient, high-speed shared memory workspace.\n- **Key Features**:\n    - **Synchronized State**: Workers read/write current layout (GDL), rules (Lua), and interface (React) configurations in real-time.\n    - **Persistence Mapping**: Reconstituted from the persistent Database and Assets Folder on new multi-turn chat generation turns, maintaining perfect contextual memory of the conversation.\n\n### 2. Hierarchical Orchestration Diagram\n\n```mermaid\nsequenceDiagram\n    autonumber\n    actor User\n    participant S as Supervisor Agent\n    participant C as Canvas Agent\n    participant L as Logic Agent\n    participant U as UI Agent\n    participant V as Sandbox Validator\n\n    User->>S: Prompt: \"Make exploding kittens card game\"\n    Note over S: Supervisor creates/loads Blackboard\n    S->>C: Write GDL Canvas elements (zones, pieces)\n    C-->>S: Return GDL Layout\n    S->>L: Write Lua State Machine (Setup, moves)\n    L-->>S: Return Lua Code\n    S->>U: Generate React Component (Konva board)\n    U-->>S: Return React Code\n    S->>V: Execute Sandbox Simulation & DOM checks\n    alt validation failed\n        V-->>S: Compiler/Mismatch errors\n        S->>L: Re-execute/Self-correct specific bug (retry)\n    else validation passed\n        V-->>S: 100% OK\n        S->>User: \"Game Generation Successful!\"\n    end\n```\n\n## Agent Orchestration & Configuration\n\n### LLM Providers\nThe platform utilizes a unified `llmclient` (`internal/llmclient`) that abstracts multiple providers:\n- **Google Gemini (Default)**: Integrated via Vertex AI or the Google AI SDK. Default models include `gemini-2.5-pro` for complex logic and `gemini-2.5-flash` for UI tasks.\n- **AWS Bedrock**: Supports Anthropic Claude models (e.g., `claude-3-5-sonnet`) for high-precision generation.\n- **DeepSeek**: Uses DeepSeek's OpenAI-compatible chat completions API. Configure with `DEEPSEEK_API_KEY`, optional `DEEPSEEK_BASE_URL`, and role-specific model overrides such as `DEEPSEEK_LOGIC_MODEL`.\n- **Ollama**: Uses Ollama's local OpenAI-compatible chat completions API. Configure with `OLLAMA_BASE_URL` and role-specific model overrides such as `OLLAMA_LOGIC_MODEL`.\n\nSelection is controlled via the `LLM_PROVIDER` environment variable (`gemini`, `vertex`, `bedrock`, `nvidia`, `deepseek`, `ollama`, `olama`, `local`).\n\n### Agent Telemetry & Observability (Arize Phoenix)\nThe platform uses **Arize Phoenix** for tracing, telemetry, and observability of all LLM and agent activities instead of separate ADK offline evaluation harnesses.\n- **Traces**: Tracing is integrated using OpenInference semantic conventions.\n- **Viewing & CLI**: Access the Phoenix dashboard at `http://localhost:6006` during local development to inspect model inputs, outputs, tokens, latency, and agent trace lineages. Always use the Phoenix CLI (`px`) for all trace-related tasks (e.g. `px trace get <trace-id> --endpoint http://localhost:6006 --project pandya.ai`).\n\n\n## Development Setup & Commands\nThe project uses `make` for orchestration.\n\n### Local Development\n- Start database and proxy (for local dev): `make infra-up`\n- Run backend locally: `make run-backend`\n- Run UI locally: `make run-ui`\n- Apply database migrations: `make migrate`\n\n### Docker (Full Stack)\n- Start all services: `make up`\n- Rebuild and start: `make rebuild`\n- Stop all services: `make down`\n\n### Code Generation & Testing\n- Generate Protobuf/ConnectRPC code: `make proto`\n- Run backend unit tests: `make test`\n- Run Playwright E2E tests: `make e2e`\n\n\n\n## Code Style & Architectural Guidelines\n\n### General Architecture\n- **Terminology (Game vs. Match)**: \n  - **Game Definition (or Game)**: Refers to the authored rules, layout, and configuration of a game (e.g., Tic-Tac-Toe, Chess). Managed by the `GameAuthorService` and the external SDK/MCP tools.\n  - **Match**: Refers to a specific, playable instance of a game happening in the lobby between users. Managed by the `LobbyService` (`Match`, `CreateMatch`, `JoinMatch`).\n- **Single Source of Truth**: The Go backend manages all game states in-memory for active sessions and persists to PostgreSQL.\n- **Protocol**: Always use ConnectRPC for frontend-backend communication.\n- **Game Logic**: Game definitions have migrated from a JSON-based format to Lua scripts. Ensure any logic-related code respects the Lua VM integration.\n\n### Game Variations\n- **Definition**: Variations allow users to toggle custom rules, alternative board configurations, and piece sets before starting a match (e.g., \"Fast Mode\", \"Expansion Sets\"). They are defined in the GDL JSON schema under the `variations` array.\n- **GDL Configuration (Class Inheritance Model)**:\n  - Variations behave like a class inheritance structure against the base \"default\" configuration. The JSON stores additional items or override items directly in the variation objects. \n  - `GameVariation` allows defining overrides for the `board` layout, overriding `setup` properties, and injecting additional `pieces` and `paths`. \n  - When a variant is active, its state is merged on top of the default base state. Pieces and spatial entities can also be dynamically controlled using the `required_variations` string array field. If empty, the entity is part of the base layer. If populated, it is exclusively present when that variation is active.\n- **Lua Implementation**: The Lua Engine dynamically injects active variation IDs into the environment at runtime.\n  - You can check if a variation is active by using the global helper function: `if game.get_variation(\"variant_id\") then ... end`.\n  - Alternatively, you can get all active variations using: `local vars = game.get_variations()`.\n- **UI Implementation**: The UI does not need to handle the variation state manually; the backend engine seamlessly toggles the variations and handles the rule alterations and board overrides automatically.\n\n### Frontend (UI/UX)\n- **Framework**: React.\n- **Design System**: Strictly adhere to Google Material Design principles. Focus on visual excellence, sleek dark modes, micro-animations, and glassmorphism.\n- **Styling**: Prefer Vanilla CSS (`index.css`) with CSS variables/tokens. Do not use TailwindCSS unless explicitly requested.\n- **Components**: The UI Agent dynamically generates React components (e.g., custom boards). These must adhere to defined game Action IDs.\n- **Performance Guidelines**:\n  - Enforce concurrency: Batch parallel API calls using `Promise.all` with individual error-boundary catch handling.\n  - Enforce state/rendering memoization: Wrap expensive board logic calculations, piece positions, dimensions, and action lookup handlers in React `useMemo` hooks to prevent React-Konva rendering lag.\n  - Enforce static compilation: Prefer top-of-file static module imports over dynamic `import()` calls to allow build bundles to bundle and optimize properly.\n\n### Backend (Go)\n- Use `slog` for structured logging.\n- Prioritize determinism in the game engine by using the seeded RNG provided in the `Ctx`.\n- Ensure robust security for game actions using JWT-based authorization and `PlayerView` state redaction.\n\n## Pull Request / Commit Guidelines\n- Before committing, ensure the backend tests (`make test`) pass.\n- Verify that generated UI components function correctly within the native preview environment without causing loading errors.\n- Any changes to database schemas must be accompanied by new SQL migrations in the `migrations` folder and applied via `make migrate`.\n- **CI Dependency Setup**: Note that the CI pipeline tests UI components using `make test`. Ensure that `npm ci --legacy-peer-deps` is executed under the `ui/` directory first if manual/debugging execution is done.\n- **Environment-Independent Test Fixtures**: All files or scripts required by test cases (such as game logic Lua rule assets) must be stored in package-relative `testdata/` folders and checked in. Absolute local paths must never be used in code under test.\n"
    }
  ]
}
